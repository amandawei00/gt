import numpy as np

class Structure:
    def __init__(self, coords, paxis=[0,0,1], format='cart'):
        self.coords = coords
        self.paxis  = paxis

        if format=='cart':
            for i in coords:
                
        elif format == 'sph':

        else
    def rot(self, axis, theta):

    def ref(self, axis):

    def search_rot(self):
    def search_ref(self):

    def
